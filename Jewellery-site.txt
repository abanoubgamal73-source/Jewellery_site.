<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>صائغ القاهرة – مجوهرات يدوية</title>

    <!-- ===== Google Fonts ===== -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Cairo:wght@300;400;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />

    <style>
        /* ==========================================================
           CSS CUSTOM PROPERTIES (Mobile-First)
           ========================================================== */
        :root {
            --bg-primary: #0d0d0e;
            --bg-card: #0a0a0b;
            --gold: #c9a84c;
            --gold-dark: #b8943a;
            --gold-glow: rgba(201, 168, 76, 0.25);
            --text-primary: #f2ede6;
            --text-secondary: #a0978a;
            --border-dark: #2a251e;
            --font-arabic: 'Cairo', sans-serif;
            --font-english: 'Inter', sans-serif;
            --font-title: 'Playfair Display', serif;
        }

        /* ===== RESET & BASE (Mobile-First) ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: var(--bg-primary);
            color: var(--text-primary);
            font-family: var(--font-arabic);
            font-weight: 300;
            line-height: 1.8;
            -webkit-font-smoothing: antialiased;
            padding: 12px 0 0 0;
            transition: opacity 0.4s ease;
        }

        .container {
            max-width: 100%;
            padding: 0 16px;
            margin: 0 auto;
        }

        /* ===== شريط التمرير ===== */
        ::-webkit-scrollbar {
            width: 6px;
            background: var(--bg-primary);
        }
        ::-webkit-scrollbar-thumb {
            background: var(--gold);
            border-radius: 10px;
        }

        /* ==========================================================
           HEADER (Mobile-First)
           ========================================================== */
        header {
            padding: 14px 0 12px 0;
            border-bottom: 2px solid var(--gold);
            background: rgba(13, 13, 14, 0.92);
            backdrop-filter: blur(8px);
            position: sticky;
            top: 0;
            z-index: 100;
            margin-bottom: 20px;
        }

        .header-inner {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
        }

        .logo {
            font-family: var(--font-title);
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--gold);
            letter-spacing: 0.06em;
            text-decoration: none;
            cursor: pointer;
        }

        .logo small {
            font-size: 0.75rem;
            font-weight: 300;
            color: var(--text-secondary);
            font-family: var(--font-arabic);
            margin-right: 6px;
            letter-spacing: 0.04em;
        }

        nav {
            display: flex;
            gap: 16px;
            font-family: var(--font-arabic);
            font-weight: 400;
            font-size: 0.85rem;
            flex-wrap: wrap;
            justify-content: center;
        }

        nav a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: color 0.25s;
            letter-spacing: 0.04em;
            position: relative;
            cursor: pointer;
        }

        nav a::after {
            content: '';
            position: absolute;
            bottom: -4px;
            right: 0;
            width: 0;
            height: 1px;
            background: var(--gold);
            transition: width 0.3s ease;
        }

        nav a:hover {
            color: var(--text-primary);
        }
        nav a:hover::after {
            width: 100%;
        }

        nav a.active {
            color: var(--text-primary);
        }
        nav a.active::after {
            width: 100%;
        }

        /* ==========================================================
           PAGES CONTAINER
           ========================================================== */
        .page {
            display: none;
            animation: fadePage 0.4s ease;
        }

        .page.active {
            display: block;
        }

        @keyframes fadePage {
            from {
                opacity: 0;
                transform: translateY(12px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* ==========================================================
           PAGE: HOME
           ========================================================== */
        .hero {
            min-height: 75vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
            background: radial-gradient(ellipse at 50% 50%, rgba(201, 168, 76, 0.08) 0%, transparent 70%), var(--bg-primary);
            padding: 40px 20px;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, rgba(201, 168, 76, 0.05) 0%, transparent 70%);
            border-radius: 50%;
            pointer-events: none;
            z-index: 0;
        }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 800px;
        }

        .hero-content .gold-tag {
            font-family: var(--font-english);
            font-size: 0.75rem;
            letter-spacing: 0.3em;
            color: var(--gold);
            text-transform: uppercase;
            margin-bottom: 1.5rem;
            font-weight: 500;
        }

        .hero-content h1 {
            font-family: var(--font-title);
            font-size: 2.8rem;
            font-weight: 700;
            line-height: 1.1;
            letter-spacing: 0.02em;
            color: var(--text-primary);
            margin-bottom: 1rem;
        }

        .hero-content h1 span {
            color: var(--gold);
        }

        .hero-content .sub-line {
            font-size: 1.1rem;
            color: var(--text-secondary);
            font-weight: 300;
            letter-spacing: 0.04em;
            margin-bottom: 2.5rem;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
        }

        .hero-buttons {
            display: flex;
            gap: 16px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn-gold {
            display: inline-block;
            padding: 14px 32px;
            border: 1.5px solid var(--gold);
            color: var(--gold);
            font-family: var(--font-arabic);
            font-weight: 600;
            font-size: 0.95rem;
            letter-spacing: 0.06em;
            text-decoration: none;
            transition: all 0.35s ease;
            background: transparent;
            cursor: pointer;
            border-radius: 40px;
        }

        .btn-gold:hover {
            background: rgba(201, 168, 76, 0.10);
            color: var(--text-primary);
            box-shadow: 0 0 30px var(--gold-glow);
        }

        .btn-gold-solid {
            background: var(--gold);
            color: var(--bg-primary);
            border-color: var(--gold);
        }

        .btn-gold-solid:hover {
            background: transparent;
            color: var(--gold);
            box-shadow: 0 0 30px var(--gold-glow);
        }

        .btn-gold i {
            margin-left: 8px;
        }

        /* ==========================================================
           PAGE: SHOP (المتجر)
           ========================================================== */
        .section-label {
            font-family: var(--font-english);
            font-size: 0.7rem;
            letter-spacing: 0.2em;
            color: var(--gold);
            text-transform: uppercase;
            margin-bottom: 0.2rem;
            font-weight: 500;
        }

        .section-title {
            font-family: var(--font-title);
            font-weight: 700;
            font-size: 1.8rem;
            letter-spacing: 0.02em;
            color: var(--text-primary);
            margin-bottom: 0.2rem;
        }

        .gold-divider {
            width: 60px;
            height: 2px;
            background: var(--gold);
            margin: 0.6rem 0 1.8rem 0;
        }

        .filter-bar {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 10px;
            margin-bottom: 30px;
            padding: 6px 0;
        }

        .filter-btn {
            background: transparent;
            border: 1.5px solid var(--border-dark);
            color: var(--text-secondary);
            padding: 8px 20px;
            font-family: var(--font-arabic);
            font-size: 0.85rem;
            font-weight: 500;
            border-radius: 40px;
            cursor: pointer;
            transition: all 0.3s ease;
            letter-spacing: 0.04em;
        }

        .filter-btn:hover {
            border-color: var(--gold);
            color: var(--text-primary);
        }

        .filter-btn.active {
            background: var(--gold);
            border-color: var(--gold);
            color: var(--bg-primary);
            box-shadow: 0 0 20px var(--gold-glow);
        }

        .product-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 22px;
            margin-bottom: 50px;
        }

        .product-card {
            background: var(--bg-card);
            border-radius: 6px;
            overflow: hidden;
            transition: all 0.3s ease;
            border: 1px solid var(--border-dark);
            display: flex;
            flex-direction: column;
        }

        .product-card:hover {
            transform: translateY(-4px);
            border-color: var(--gold);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.5), 0 0 20px var(--gold-glow);
        }

        .glow-wrapper {
            position: relative;
            background: #080808;
            overflow: hidden;
            aspect-ratio: 1 / 1;
            width: 100%;
        }

        .glow-wrapper::before {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at 50% 50%, var(--gold-glow) 0%, transparent 70%);
            opacity: 0.4;
            transition: opacity 0.3s ease;
            pointer-events: none;
            z-index: 2;
        }

        .product-card:hover .glow-wrapper::before {
            opacity: 0.8;
        }

        .glow-wrapper img {
            display: block;
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(0.75) contrast(1.1);
            transition: filter 0.3s ease;
            position: relative;
            z-index: 1;
        }

        .product-card:hover .glow-wrapper img {
            filter: brightness(0.85) contrast(1.15);
        }

        .product-info {
            padding: 16px 18px 20px 18px;
            background: var(--bg-card);
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .product-name {
            font-family: var(--font-title);
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--text-primary);
            letter-spacing: 0.03em;
            margin-bottom: 2px;
        }

        .product-stone {
            font-size: 0.75rem;
            color: var(--gold);
            font-weight: 400;
            margin-bottom: 6px;
        }

        .product-price {
            font-family: var(--font-english);
            font-weight: 600;
            color: var(--text-primary);
            font-size: 1rem;
            margin-bottom: 14px;
            margin-top: auto;
        }

        .btn-whatsapp {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            background: transparent;
            border: 1.5px solid var(--gold);
            color: var(--gold);
            padding: 10px 0;
            font-family: var(--font-arabic);
            font-weight: 600;
            font-size: 0.85rem;
            border-radius: 40px;
            text-decoration: none;
            transition: all 0.3s ease;
            width: 100%;
            letter-spacing: 0.04em;
        }

        .btn-whatsapp i {
            font-size: 1rem;
        }

        .btn-whatsapp:hover {
            background: var(--gold);
            color: var(--bg-primary);
            box-shadow: 0 0 20px var(--gold-glow);
        }

        .no-products {
            grid-column: 1 / -1;
            text-align: center;
            padding: 50px 20px;
            color: var(--text-secondary);
            font-size: 1.1rem;
            font-family: var(--font-title);
        }

        /* ==========================================================
           PAGE: PRODUCT DETAIL
           ========================================================== */
        .breadcrumb {
            font-size: 0.75rem;
            color: var(--text-secondary);
            margin-bottom: 20px;
            font-family: var(--font-english);
            letter-spacing: 0.04em;
            overflow-x: auto;
            white-space: nowrap;
            padding-bottom: 4px;
        }

        .breadcrumb a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: color 0.25s;
            cursor: pointer;
        }

        .breadcrumb a:hover {
            color: var(--gold);
        }

        .breadcrumb .sep {
            margin: 0 6px;
            color: var(--border-dark);
        }

        .product-detail {
            display: flex;
            flex-direction: column;
            gap: 30px;
            margin-bottom: 50px;
        }

        .product-gallery {
            width: 100%;
        }

        .main-image-wrapper {
            position: relative;
            background: #080808;
            border-radius: 6px;
            overflow: hidden;
            aspect-ratio: 1 / 1;
            border: 1px solid var(--border-dark);
            margin-bottom: 14px;
        }

        .main-image-wrapper::before {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at 50% 50%, var(--gold-glow) 0%, transparent 70%);
            opacity: 0.5;
            pointer-events: none;
            z-index: 2;
            transition: opacity 0.3s ease;
        }

        .main-image-wrapper:hover::before {
            opacity: 0.8;
        }

        .main-image-wrapper img {
            display: block;
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(0.80) contrast(1.05);
            transition: filter 0.3s ease;
            position: relative;
            z-index: 1;
        }

        .main-image-wrapper:hover img {
            filter: brightness(0.90) contrast(1.1);
        }

        .thumbnail-strip {
            display: flex;
            gap: 10px;
            flex-wrap: nowrap;
            overflow-x: auto;
            padding-bottom: 6px;
        }

        .thumbnail-strip::-webkit-scrollbar {
            height: 3px;
        }
        .thumbnail-strip::-webkit-scrollbar-thumb {
            background: var(--gold);
            border-radius: 10px;
        }

        .thumbnail-item {
            flex: 0 0 70px;
            aspect-ratio: 1 / 1;
            border-radius: 4px;
            overflow: hidden;
            border: 2px solid transparent;
            cursor: pointer;
            transition: all 0.25s ease;
            background: #080808;
            opacity: 0.6;
        }

        .thumbnail-item:hover {
            opacity: 1;
        }

        .thumbnail-item.active {
            border-color: var(--gold);
            opacity: 1;
            box-shadow: 0 0 16px var(--gold-glow);
        }

        .thumbnail-item img {
            display: block;
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(0.75) contrast(1.05);
        }

        .product-info-detail .category-tag {
            font-family: var(--font-english);
            font-size: 0.7rem;
            letter-spacing: 0.2em;
            color: var(--gold);
            text-transform: uppercase;
            margin-bottom: 6px;
            font-weight: 500;
        }

        .product-info-detail .product-name {
            font-family: var(--font-title);
            font-size: 2rem;
            font-weight: 700;
            letter-spacing: 0.02em;
            color: var(--text-primary);
            margin-bottom: 10px;
            line-height: 1.2;
        }

        .detail-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            background: var(--bg-card);
            padding: 14px 16px;
            border-radius: 6px;
            border: 1px solid var(--border-dark);
            margin-bottom: 20px;
        }

        .detail-item .label {
            display: block;
            font-size: 0.65rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--text-secondary);
            font-family: var(--font-english);
            font-weight: 400;
        }

        .detail-item .value {
            display: block;
            font-size: 0.9rem;
            font-weight: 500;
            color: var(--text-primary);
            margin-top: 2px;
            font-family: var(--font-arabic);
        }

        .detail-item .value.gold-color {
            color: var(--gold);
        }

        .product-description {
            color: var(--text-secondary);
            font-size: 0.95rem;
            line-height: 1.9;
            margin-bottom: 22px;
        }

        .product-price-detail {
            font-family: var(--font-english);
            font-weight: 700;
            font-size: 2rem;
            color: var(--gold);
            letter-spacing: 0.04em;
            margin-bottom: 24px;
            padding-bottom: 16px;
            border-bottom: 1px solid var(--border-dark);
        }

        .action-group {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .btn-primary {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            background: var(--gold);
            border: 1.5px solid var(--gold);
            color: var(--bg-primary);
            padding: 14px 20px;
            font-family: var(--font-arabic);
            font-weight: 700;
            font-size: 0.95rem;
            border-radius: 40px;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.3s ease;
            letter-spacing: 0.04em;
            width: 100%;
        }

        .btn-primary:hover {
            background: transparent;
            color: var(--gold);
            box-shadow: 0 0 30px var(--gold-glow);
        }

        .btn-secondary {
            display: inline-flex;
            align-items: center;
          